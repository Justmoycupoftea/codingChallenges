// You will be given an array a and a value x. All you need to do 

// is check whether the provided array contains the value.

// Array can contain numbers or strings. X can be either.

// Return true if the array contains the value, false if not.

//PREP
//Parameters
array = a, value = x
//Returns
checking if array, a contains the value, x
//Examples
if(x ) //didn't need to use condtional if x == to string || num
return a.includes(x) ? true : false
//Psuedo code

function check(a, x) {
    // your code here
    return a.includes(x) ? true : false
  
}